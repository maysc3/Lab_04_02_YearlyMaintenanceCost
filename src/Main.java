public class Main {
    public static void main(String[] args)
    {
        double springMain = 130.61;
        double summerMain = 95.47;
        double fallMain = 241.39;
        double winterMain = 172.65;
        double totalMain = springMain + summerMain + fallMain + winterMain;

        System.out.println("The respective maintenance costs for the Spring, Summer, Fall, and Winter are " + springMain + ", " + summerMain + ", " + fallMain + ", " + winterMain + ". Which total to " + totalMain + " for the year.");
    }
}